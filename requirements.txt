# Core
numpy>=1.26,<3
pandas>=2.0
pyarrow>=14.0            # parquet + predicate-pushdown filtered reads
scikit-learn>=1.3        # StratifiedGroupKFold, LogisticRegression, TF-IDF
scipy>=1.11
pyyaml>=6.0
joblib>=1.3
matplotlib>=3.7
tqdm>=4.66

# Deep learning (Bio_ClinicalBERT embedding extraction)
# torch is preinstalled on Colab with the correct CUDA build -- do NOT pip
# install a different torch there or you may break the GPU runtime.
torch>=2.1               # provided by Colab; listed for local/CI installs
transformers>=4.38
